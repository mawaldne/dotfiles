[user]
	name = Mike Waldner
	email = mike.waldner@redfall.com
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
[alias]
	stat = status -sb
    aa = add --all
    di = diff
    dc = diff --cached
    amend = commit --amend
	stat = status -sb
    pretty = "!. ~/.githelpers && pretty_git_log"
    delete-merged = "!. ~/.githelpers && delete_merged_branches"
[core]
    excludesfile = /Users/mawaldne/.gitignore
    autocrlf = input
[branch]
	autosetuprebase = always
[credential]
	helper = osxkeychain
[push]
	default = simple
[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f

